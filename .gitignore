scone_images.json
Cargo.lock

# Rendered manifests, Values.yaml files, sessions and other files generated by
# the demos. Only the *.template.yaml sources (and hand-written non-template
# inputs listed below) are tracked.
demos/**/*.yaml
!demos/**/*.template.yaml
!demos/software-updates/manifests/scone-secret.yaml

storage.json
target
flask-redis-demo.json

docs/superpowers/
.worktrees/

*.private
*.pub
*.pem
